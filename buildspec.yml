version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies..."
      # Uncomment if you have requirements.txt
      # - pip install -r requirements.txt

  build:
    commands:
      - echo "Build phase - add your build commands here if needed"

artifacts:
  files:
    - '**/*'
